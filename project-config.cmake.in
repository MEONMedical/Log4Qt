get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(${SELF_DIR}/@PROJECT_NAME@-targets.cmake)

string(TOUPPER @PROJECT_NAME@ PROJECT_TO_UPPER)

add_definitions(-D${PROJECT_TO_UPPER} -D${PROJECT_TO_UPPER}_IMPORTS)

set(${PROJECT_TO_UPPER}_INCLUDE_DIRS @INSTALL_PROJECT_INCLUDE_DIR@)
#get_filename_component(${PROJECT_TO_UPPER}_INCLUDE_DIRS "@INSTALL_PROJECT_INCLUDE_DIR@" ABSOLUTE)